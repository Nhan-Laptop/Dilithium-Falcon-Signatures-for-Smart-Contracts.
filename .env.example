# Copy this to .env and fill values when running the demo
# RPC endpoint (Hardhat local node)
RPC=http://127.0.0.1:8545
# PRIVATE KEY of attestor account from local hardhat node (do NOT use prod keys)
ATTESTOR_PRIV=
# Address of deployed PQAttest contract
CONTRACT_ADDRESS=
# User address to reference in the attestation
USER_ADDRESS=
# Optional message for the payload (default withdraw:100)
MESSAGE=withdraw:100
